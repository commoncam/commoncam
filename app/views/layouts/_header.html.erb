<header>
  <nav>
    <%= link_to 'Home', root_path %>
    <% if user_signed_in? %>
      <%= link_to 'Sign Out', sign_out_path %>
    <% else %>
      <%= link_to 'Sign In', sign_in_path %>
    <% end %>
  </nav>
</header>

